'use strict';
var { router, middlewares } = require('../core')

router.get('/dropbox-backup/settings', (req, res, next)=>{
})

router.post('/dropbox-backup/settings', (req, res, next)=>{
})

router.post('/dropbox-backup/test-settings', (req, res, next)=>{
})

module.exports = router
